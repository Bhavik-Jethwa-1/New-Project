# Application Settings
APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:qju23yki77AlH3P/cjrUiQrUNYY5weglOzyYKtzAVHo=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000  
# Ensure Laravel serves from 127.0.0.1

# Logging Configuration
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ssksct
DB_USERNAME=root
DB_PASSWORD=

# Cache and Session
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Redis Configuration (Optional)
MEMCACHED_HOST=127.0.0.1
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration (Optional)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS S3 Configuration (If needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher Configuration (If using WebSockets)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Vue.js + Laravel API Connection
CORS_ALLOWED_ORIGINS=http://localhost:8080  
# Allow Vue frontend
SESSION_DOMAIN=localhost
SANCTUM_STATEFUL_DOMAINS=localhost:8080  
# Required for Sanctum Auth

# Vite Environment Variables (For Vue + Laravel)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

PASSPORT_PASSWORD_GRANT_CLIENT_ID=4
PASSPORT_PASSWORD_GRANT_CLIENT_SECRET=azuzahMlpffj0Fjrh2bQF9ZsPTsKIWOBWbaawm8P